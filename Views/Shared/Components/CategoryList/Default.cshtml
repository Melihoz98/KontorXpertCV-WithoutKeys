<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Category List</title>
    <style>
        body {
            margin: 0;
            font-family: 'Raleway', sans-serif;
        }

        .navbar {
            list-style-type: none;
            padding: 0;
            margin: 0;
            display: flex;
            justify-content: center;
            padding-bottom: 23px; /* Adjust this value as needed */
        }

            .navbar li {
                display: inline-block;
                margin: 0 10px; /* Adjust the horizontal spacing here */
                position: relative;
            }

            .navbar a {
                text-decoration: none;
                color: #000;
                padding: 10px 15px; /* Optional: Add padding for better click area */
                display: inline-block;
            }

        .top-category {
            font-weight: 300;
            cursor: pointer;
        }

            .top-category:hover::after {
                content: '';
                position: absolute;
                left: 0;
                bottom: -2px;
                height: 2px;
                width: 100%;
                background-color: #000;
                text-decoration: underline;
            }

            .top-category:hover{
                text-decoration: underline;
            }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 300px; /* Adjust the width based on content */
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
            padding: 10px;
            margin: 0;
            column-count: 2; /* Create a multi-column layout */
            column-gap: 20px; /* Space between columns */
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .dropdown-content li {
            padding: 8px 0;
        }

        .dropdown-content ul {
            padding-left: 0;
            margin: 0;
            list-style-type: none;
        }

        .dropdown-content strong {
            font-weight: bold;
            display: block;
            margin-bottom: 5px; /* Space below the parent category name */
        }

        .dropdown-content a {
            text-decoration: none;
            color: #000;
        }

            .dropdown-content a:hover {
                text-decoration: underline;
            }


    </style>
</head>
<body>
    <ul class="navbar">
        <li class="dropdown">
            <a class="top-category" href="#">BORD</a>
            <ul class="dropdown-content">
                @if (ViewBag.Bord != null)
                {
                    @foreach (var parentCategory in ViewBag.Bord)
                    {
                        <li>
                            <!-- Link to fetch products based on the parent category -->
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
        <li class="dropdown">
            <a class="top-category" href="#">STOL</a>
            <ul class="dropdown-content">
                @if (ViewBag.Stol != null)
                {
                    @foreach (var parentCategory in ViewBag.Stol)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
        <li class="dropdown">
            <a class="top-category" href="#">OPBEVARING</a>
            <ul class="dropdown-content">
                @if (ViewBag.Opbevaring != null)
                {
                    @foreach (var parentCategory in ViewBag.Opbevaring)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
        <li class="dropdown">
            <a class="top-category" href="#">AFSKÆRMING & LYDDÆMPING</a>
            <ul class="dropdown-content">
                @if (ViewBag.AfskærmingOgLyddæmpning != null)
                {
                    @foreach (var parentCategory in ViewBag.AfskærmingOgLyddæmpning)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
        <li class="dropdown">
            <a class="top-category" href="#">BELYSNING</a>
            <ul class="dropdown-content">
                @if (ViewBag.Belysning != null)
                {
                    @foreach (var parentCategory in ViewBag.Belysning)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
        <li class="dropdown">
            <a class="top-category" href="#">TAVLER</a>
            <ul class="dropdown-content">
                @if (ViewBag.Tavler != null)
                {
                    @foreach (var parentCategory in ViewBag.Tavler)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
        <li class="dropdown">
            <a class="top-category" href="#">SOFAER & LOUNGE</a>
            <ul class="dropdown-content">
                @if (ViewBag.SofaerOgLænestole != null)
                {
                    @foreach (var parentCategory in ViewBag.SofaerOgLænestole)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Product", new { parentCategoryID = parentCategory.ParentCategoryID })">
                                <strong>@parentCategory.ParentCategoryName</strong>
                            </a>
                            @if (parentCategory.Categories != null)
                            {
                                <ul>
                                    @foreach (var category in parentCategory.Categories)
                                    {
                                        <li>
                                            <a href="@Url.Action("Index", "Product", new { categoryID = category.CategoryID })">
                                                @category.CategoryName
                                            </a>
                                        </li>
                                    }
                                </ul>
                            }
                        </li>
                    }
                }
            </ul>
        </li>
    </ul>

</body>
</html>
